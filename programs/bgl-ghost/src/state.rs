// PDA seed prefixes
pub const GHOST_PREFIX: &[u8] = b"ghost";

// TODO: Define ghost data structures that will be stored in the NFT metadata
// Consider:
// - High score data (score, timestamp, game_id)
// - Play data (replay data, inputs, etc.)
// - Expiration info (expiry_timestamp, is_expired)
// - Payout info (payout_amount, recipient, etc.)
// - Owner verification data
