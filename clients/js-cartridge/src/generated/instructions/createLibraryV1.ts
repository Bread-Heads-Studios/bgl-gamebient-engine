/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findLibraryPda } from '../../hooked';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CreateLibraryV1InstructionAccounts = {
  /** The library authority PDA derived from the curator */
  library?: PublicKey | Pda;
  /** The new Core Group account */
  group: Signer;
  /** The curator who owns the library */
  curator?: Signer;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The mpl core program */
  mplCoreProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CreateLibraryV1InstructionData = {
  discriminator: number;
  name: string;
  uri: string;
};

export type CreateLibraryV1InstructionDataArgs = { name: string; uri: string };

export function getCreateLibraryV1InstructionDataSerializer(): Serializer<
  CreateLibraryV1InstructionDataArgs,
  CreateLibraryV1InstructionData
> {
  return mapSerializer<
    CreateLibraryV1InstructionDataArgs,
    any,
    CreateLibraryV1InstructionData
  >(
    struct<CreateLibraryV1InstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['uri', string()],
      ],
      { description: 'CreateLibraryV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 6 })
  ) as Serializer<
    CreateLibraryV1InstructionDataArgs,
    CreateLibraryV1InstructionData
  >;
}

// Args.
export type CreateLibraryV1InstructionArgs = CreateLibraryV1InstructionDataArgs;

// Instruction discriminator.
export const createLibraryV1InstructionDiscriminator = 6;

// Instruction.
export function createLibraryV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  input: CreateLibraryV1InstructionAccounts & CreateLibraryV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglCartridge',
    'CART9hmcuf38a58NCYhRJmtGXJjh16eXmLr9hmhAqPZo'
  );

  // Accounts.
  const resolvedAccounts = {
    library: {
      index: 0,
      isWritable: false as boolean,
      value: input.library ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    curator: {
      index: 2,
      isWritable: false as boolean,
      value: input.curator ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    mplCoreProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CreateLibraryV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.curator.value) {
    resolvedAccounts.curator.value = context.identity;
  }
  if (!resolvedAccounts.library.value) {
    resolvedAccounts.library.value = findLibraryPda(context, {
      curator: expectPublicKey(resolvedAccounts.curator.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCreateLibraryV1InstructionDataSerializer().serialize(
    resolvedArgs as CreateLibraryV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
