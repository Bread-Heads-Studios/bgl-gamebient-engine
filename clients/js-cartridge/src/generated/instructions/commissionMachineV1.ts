/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  string,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findMachinePda } from '../../hooked';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  expectSome,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CommissionMachineV1InstructionAccounts = {
  /** The new machine asset account */
  machine?: PublicKey | Pda;
  /** The Core machine collection */
  machineCollection: PublicKey | Pda;
  /** The owner of the machine */
  owner: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority signing for account creation */
  authority?: Signer;
  /** The mpl core program */
  mplCoreProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type CommissionMachineV1InstructionData = {
  discriminator: number;
  name: string;
  uri: string;
};

export type CommissionMachineV1InstructionDataArgs = {
  name: string;
  uri: string;
};

export function getCommissionMachineV1InstructionDataSerializer(): Serializer<
  CommissionMachineV1InstructionDataArgs,
  CommissionMachineV1InstructionData
> {
  return mapSerializer<
    CommissionMachineV1InstructionDataArgs,
    any,
    CommissionMachineV1InstructionData
  >(
    struct<CommissionMachineV1InstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['uri', string()],
      ],
      { description: 'CommissionMachineV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 0 })
  ) as Serializer<
    CommissionMachineV1InstructionDataArgs,
    CommissionMachineV1InstructionData
  >;
}

// Args.
export type CommissionMachineV1InstructionArgs =
  CommissionMachineV1InstructionDataArgs;

// Instruction.
export function commissionMachineV1(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: CommissionMachineV1InstructionAccounts &
    CommissionMachineV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglCartridge',
    'CART9hmcuf38a58NCYhRJmtGXJjh16eXmLr9hmhAqPZo'
  );

  // Accounts.
  const resolvedAccounts = {
    machine: {
      index: 0,
      isWritable: true as boolean,
      value: input.machine ?? null,
    },
    machineCollection: {
      index: 1,
      isWritable: true as boolean,
      value: input.machineCollection ?? null,
    },
    owner: {
      index: 2,
      isWritable: false as boolean,
      value: input.owner ?? null,
    },
    payer: {
      index: 3,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 4,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    mplCoreProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: CommissionMachineV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.machine.value) {
    resolvedAccounts.machine.value = findMachinePda(context, {
      machineCollection: expectPublicKey(
        resolvedAccounts.machineCollection.value
      ),
      name: expectSome(resolvedArgs.name),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCommissionMachineV1InstructionDataSerializer().serialize(
    resolvedArgs as CommissionMachineV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
