/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findLibraryPda } from '../../hooked';
import {
  PickPartial,
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPda,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ListGameV1InstructionAccounts = {
  /** The library authority PDA derived from the curator */
  library?: Pda;
  /** The Core Group to add the game to */
  group: PublicKey | Pda;
  /** The game Collection account */
  game: PublicKey | Pda;
  /** The curator who owns the library */
  curator?: Signer;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The mpl core program */
  mplCoreProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ListGameV1InstructionData = {
  discriminator: number;
  libraryBump: number;
};

export type ListGameV1InstructionDataArgs = { libraryBump: number };

export function getListGameV1InstructionDataSerializer(): Serializer<
  ListGameV1InstructionDataArgs,
  ListGameV1InstructionData
> {
  return mapSerializer<
    ListGameV1InstructionDataArgs,
    any,
    ListGameV1InstructionData
  >(
    struct<ListGameV1InstructionData>(
      [
        ['discriminator', u8()],
        ['libraryBump', u8()],
      ],
      { description: 'ListGameV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 8 })
  ) as Serializer<ListGameV1InstructionDataArgs, ListGameV1InstructionData>;
}

// Args.
export type ListGameV1InstructionArgs = PickPartial<
  ListGameV1InstructionDataArgs,
  'libraryBump'
>;

// Instruction discriminator.
export const listGameV1InstructionDiscriminator = 8;

// Instruction.
export function listGameV1(
  context: Pick<Context, 'eddsa' | 'identity' | 'payer' | 'programs'>,
  input: ListGameV1InstructionAccounts & ListGameV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglCartridge',
    'CART9hmcuf38a58NCYhRJmtGXJjh16eXmLr9hmhAqPZo'
  );

  // Accounts.
  const resolvedAccounts = {
    library: {
      index: 0,
      isWritable: false as boolean,
      value: input.library ?? null,
    },
    group: {
      index: 1,
      isWritable: true as boolean,
      value: input.group ?? null,
    },
    game: { index: 2, isWritable: true as boolean, value: input.game ?? null },
    curator: {
      index: 3,
      isWritable: false as boolean,
      value: input.curator ?? null,
    },
    payer: {
      index: 4,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    mplCoreProgram: {
      index: 5,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ListGameV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.curator.value) {
    resolvedAccounts.curator.value = context.identity;
  }
  if (!resolvedAccounts.library.value) {
    resolvedAccounts.library.value = findLibraryPda(context, {
      curator: expectPublicKey(resolvedAccounts.curator.value),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }
  if (!resolvedArgs.libraryBump) {
    resolvedArgs.libraryBump = expectPda(resolvedAccounts.library.value)[1];
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getListGameV1InstructionDataSerializer().serialize(
    resolvedArgs as ListGameV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
