/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  string,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findGamePda } from '../../hooked';
import {
  PickPartial,
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectSome,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type ReleaseGameV1InstructionAccounts = {
  /** The new game Collection account */
  game?: PublicKey | Pda;
  /** The account paying for the storage fees */
  payer?: Signer;
  /** The authority signing for account creation */
  authority?: Signer;
  /** The mpl core program */
  mplCoreProgram?: PublicKey | Pda;
  /** The system program */
  systemProgram?: PublicKey | Pda;
};

// Data.
export type ReleaseGameV1InstructionData = {
  discriminator: number;
  name: string;
  uri: string;
  nonce: number;
  price: bigint;
};

export type ReleaseGameV1InstructionDataArgs = {
  name: string;
  uri: string;
  nonce?: number;
  price: number | bigint;
};

export function getReleaseGameV1InstructionDataSerializer(): Serializer<
  ReleaseGameV1InstructionDataArgs,
  ReleaseGameV1InstructionData
> {
  return mapSerializer<
    ReleaseGameV1InstructionDataArgs,
    any,
    ReleaseGameV1InstructionData
  >(
    struct<ReleaseGameV1InstructionData>(
      [
        ['discriminator', u8()],
        ['name', string()],
        ['uri', string()],
        ['nonce', u8()],
        ['price', u64()],
      ],
      { description: 'ReleaseGameV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 1, nonce: value.nonce ?? 0 })
  ) as Serializer<
    ReleaseGameV1InstructionDataArgs,
    ReleaseGameV1InstructionData
  >;
}

// Args.
export type ReleaseGameV1InstructionArgs = PickPartial<
  ReleaseGameV1InstructionDataArgs,
  'nonce'
>;

// Instruction.
export function releaseGameV1(
  context: Pick<Context, 'eddsa' | 'payer' | 'programs'>,
  input: ReleaseGameV1InstructionAccounts & ReleaseGameV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglCartridge',
    'CART9hmcuf38a58NCYhRJmtGXJjh16eXmLr9hmhAqPZo'
  );

  // Accounts.
  const resolvedAccounts = {
    game: { index: 0, isWritable: true as boolean, value: input.game ?? null },
    payer: {
      index: 1,
      isWritable: true as boolean,
      value: input.payer ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    mplCoreProgram: {
      index: 3,
      isWritable: false as boolean,
      value: input.mplCoreProgram ?? null,
    },
    systemProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.systemProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: ReleaseGameV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedArgs.nonce) {
    resolvedArgs.nonce = 0;
  }
  if (!resolvedAccounts.game.value) {
    resolvedAccounts.game.value = findGamePda(context, {
      name: expectSome(resolvedArgs.name),
      nonce: expectSome(resolvedArgs.nonce),
    });
  }
  if (!resolvedAccounts.payer.value) {
    resolvedAccounts.payer.value = context.payer;
  }
  if (!resolvedAccounts.mplCoreProgram.value) {
    resolvedAccounts.mplCoreProgram.value = context.programs.getPublicKey(
      'mplCore',
      'CoREENxT6tW1HoK8ypY1SxRMZTcVPm7R94rH4PZNhX7d'
    );
    resolvedAccounts.mplCoreProgram.isWritable = false;
  }
  if (!resolvedAccounts.systemProgram.value) {
    resolvedAccounts.systemProgram.value = context.programs.getPublicKey(
      'splSystem',
      '11111111111111111111111111111111'
    );
    resolvedAccounts.systemProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getReleaseGameV1InstructionDataSerializer().serialize(
    resolvedArgs as ReleaseGameV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
