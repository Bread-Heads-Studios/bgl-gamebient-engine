/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u64,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type SlashV1InstructionAccounts = {
  /** The staking pool account */
  pool: PublicKey | Pda;
  /** The stake account to slash */
  stakeAccount: PublicKey | Pda;
  /** The authority of the pool */
  authority?: Signer;
  /** The pool's vault token account */
  vault: PublicKey | Pda;
  /** The destination for slashed tokens */
  slashDestination: PublicKey | Pda;
  /** The vault authority PDA */
  vaultAuthority: PublicKey | Pda;
  /** The SPL Token program */
  tokenProgram?: PublicKey | Pda;
};

// Data.
export type SlashV1InstructionData = {
  discriminator: number;
  padding: Array<number>;
  amount: bigint;
};

export type SlashV1InstructionDataArgs = {
  padding: Array<number>;
  amount: number | bigint;
};

export function getSlashV1InstructionDataSerializer(): Serializer<
  SlashV1InstructionDataArgs,
  SlashV1InstructionData
> {
  return mapSerializer<SlashV1InstructionDataArgs, any, SlashV1InstructionData>(
    struct<SlashV1InstructionData>(
      [
        ['discriminator', u8()],
        ['padding', array(u8(), { size: 7 })],
        ['amount', u64()],
      ],
      { description: 'SlashV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 5 })
  ) as Serializer<SlashV1InstructionDataArgs, SlashV1InstructionData>;
}

// Args.
export type SlashV1InstructionArgs = SlashV1InstructionDataArgs;

// Instruction.
export function slashV1(
  context: Pick<Context, 'identity' | 'programs'>,
  input: SlashV1InstructionAccounts & SlashV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglLegit',
    'LEG1T5rABhAnNFz62E4x2dSMjfsuDQyfc5sZjJi9tCq'
  );

  // Accounts.
  const resolvedAccounts = {
    pool: { index: 0, isWritable: true as boolean, value: input.pool ?? null },
    stakeAccount: {
      index: 1,
      isWritable: true as boolean,
      value: input.stakeAccount ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    vault: {
      index: 3,
      isWritable: true as boolean,
      value: input.vault ?? null,
    },
    slashDestination: {
      index: 4,
      isWritable: true as boolean,
      value: input.slashDestination ?? null,
    },
    vaultAuthority: {
      index: 5,
      isWritable: false as boolean,
      value: input.vaultAuthority ?? null,
    },
    tokenProgram: {
      index: 6,
      isWritable: false as boolean,
      value: input.tokenProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: SlashV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.tokenProgram.value) {
    resolvedAccounts.tokenProgram.value = context.programs.getPublicKey(
      'splToken',
      'TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA'
    );
    resolvedAccounts.tokenProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getSlashV1InstructionDataSerializer().serialize(
    resolvedArgs as SlashV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
