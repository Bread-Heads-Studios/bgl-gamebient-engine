/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  array,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  getAccountMetasAndSigners,
} from '../shared';
import {
  StakingConfig,
  StakingConfigArgs,
  getStakingConfigSerializer,
} from '../types';

// Accounts.
export type UpdatePoolV1InstructionAccounts = {
  /** The staking pool account */
  pool: PublicKey | Pda;
  /** The authority of the pool */
  authority?: Signer;
};

// Data.
export type UpdatePoolV1InstructionData = {
  discriminator: number;
  padding1: Array<number>;
  machineOwnerConfig: StakingConfig;
  gameCreatorConfig: StakingConfig;
  isActive: number;
  padding2: Array<number>;
};

export type UpdatePoolV1InstructionDataArgs = {
  padding1: Array<number>;
  machineOwnerConfig: StakingConfigArgs;
  gameCreatorConfig: StakingConfigArgs;
  isActive: number;
  padding2: Array<number>;
};

export function getUpdatePoolV1InstructionDataSerializer(): Serializer<
  UpdatePoolV1InstructionDataArgs,
  UpdatePoolV1InstructionData
> {
  return mapSerializer<
    UpdatePoolV1InstructionDataArgs,
    any,
    UpdatePoolV1InstructionData
  >(
    struct<UpdatePoolV1InstructionData>(
      [
        ['discriminator', u8()],
        ['padding1', array(u8(), { size: 7 })],
        ['machineOwnerConfig', getStakingConfigSerializer()],
        ['gameCreatorConfig', getStakingConfigSerializer()],
        ['isActive', u8()],
        ['padding2', array(u8(), { size: 7 })],
      ],
      { description: 'UpdatePoolV1InstructionData' }
    ),
    (value) => ({ ...value, discriminator: 4 })
  ) as Serializer<UpdatePoolV1InstructionDataArgs, UpdatePoolV1InstructionData>;
}

// Args.
export type UpdatePoolV1InstructionArgs = UpdatePoolV1InstructionDataArgs;

// Instruction.
export function updatePoolV1(
  context: Pick<Context, 'identity' | 'programs'>,
  input: UpdatePoolV1InstructionAccounts & UpdatePoolV1InstructionArgs
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'bglLegit',
    'LEG1T5rABhAnNFz62E4x2dSMjfsuDQyfc5sZjJi9tCq'
  );

  // Accounts.
  const resolvedAccounts = {
    pool: { index: 0, isWritable: true as boolean, value: input.pool ?? null },
    authority: {
      index: 1,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Arguments.
  const resolvedArgs: UpdatePoolV1InstructionArgs = { ...input };

  // Default values.
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getUpdatePoolV1InstructionDataSerializer().serialize(
    resolvedArgs as UpdatePoolV1InstructionDataArgs
  );

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
